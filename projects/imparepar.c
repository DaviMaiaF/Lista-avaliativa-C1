#include <stdio.h>

int main() {
   
	int calculo, syn;
	scanf("%d", &calculo);
	
	if(calculo % 2 == 0){
	 printf("%d eh par\n", calculo);
	}
	else{
	 printf("%d eh impar\n", calculo);
	}
	
    return 0;
}

